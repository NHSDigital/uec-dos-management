name: Deploy React App

on:
  workflow_call:
    inputs:
      environment:
        description: "The name of the environment to deploy into"
        required: true
        type: string
      workspace:
        description: "The name of the workspace to deploy the application into"
        required: true
        type: string
      custom_artefact_location:
        description: "If deploying a custom artefact, specify its S3 subdirectory here."
        required: false
        type: string
      domain:
        description: "The name of the code repository"
        required: true
        type: string
      multi_domain:
        description: "Domain to assume github runner role "
        required: false
        default: 'false'
        type: string
      code_directory:
        description: "The root directory for react code"
        required: false
        type: string
        default: "src/frontend"
      tag:
        description: "The tag to build and deploy from"
        required: false
        default: ""
        type: string
      commit_hash:
        description: "Git commit hash - to locate artefacts"
        required: true
        type: string
      artefact_bucket_name:
        description: "Name of s3 bucket holding domain artefacts"
        required: true
        type: string

jobs:

  derive-commit-hash:
    name: Derive Commit Hash
    runs-on: ubuntu-latest
    outputs:
        commit_hash: ${{ steps.derive-commit-hash.outputs.commit_hash }}
    steps:
      - name: "Checkout code"
        uses: NHSDigital/uec-dos-management/.github/actions/checkout-repository@task/DR-930_Implement_appdeploy_intenv
        with:
          repository: ${{ inputs.domain }}

      - name: Derive Commit Hash
        id: derive-commit-hash
        shell: bash
        run: |
          if [ ${{ inputs.commit_hash}} == 'latest' ]; then
            LATEST_HASH=$(git rev-parse --short main)
            echo "commit_hash=${LATEST_HASH}" >> $GITHUB_OUTPUT
          else
            echo "commit_hash=${{ inputs.commit_hash }}" >> $GITHUB_OUTPUT

  deploy-react-app:
    name: "Deploy react application"
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    permissions:
      id-token: write
      contents: read
    timeout-minutes: 5
    steps:
      - name: "Checkout code"
        uses: NHSDigital/uec-dos-management/.github/actions/checkout-repository@task/DR-930_Implement_appdeploy_intenv
        with:
          tag: ${{ inputs.tag }}
          repository: ${{ inputs.domain }}

      - name: Configure AWS Credentials
        uses: NHSDigital/uec-dos-management/.github/actions/configure-credentials@task/DR-930_Implement_appdeploy_intenv
        with:
          aws_account_id: ${{ secrets.ACCOUNT_ID }}
          aws_region: ${{ vars.AWS_REGION }}
          multi_domain: ${{ inputs.multi_domain }}

      - name: Derive spa bucket
        id: derive_spa_bucket
        uses: NHSDigital/uec-dos-management/.github/actions/derive-frontend-bucket@task/DR-930_Implement_appdeploy_intenv
        with:
          environment: ${{ inputs.environment }}
          workspace: ${{ inputs.workspace }}
          domain: ${{ inputs.domain }}

      - name: Derive cloudfront dist id
        id: derive_cloudfront_dist_id
        uses: NHSDigital/uec-dos-management/.github/actions/derive-cloudfront-dist-id@task/DR-930_Implement_appdeploy_intenv
        with:
          environment: ${{ inputs.environment }}
          workspace: ${{ inputs.workspace }}
          domain: ${{ inputs.domain }}

      - name: Deploy react application
        uses: NHSDigital/uec-dos-management/.github/actions/react-app-deploy@task/DR-930_Implement_appdeploy_intenv
        with:
          code_directory: ${{ inputs.code_directory }}
          spa_bucket_name: ${{ steps.derive_spa_bucket.outputs.spa_bucket_name }}
          environment: ${{ inputs.environment }}
          workspace: ${{ inputs.workspace }}
          custom_artefact_location: ${{ inputs.custom_artefact_location }}
          artefact_bucket_name: ${{ inputs.artefact_bucket_name }}
          commit_hash: ${{ needs.derive-commit-hash.outputs.commit_hash }}

      - name: Invalidate CloudFront Cache
        uses: NHSDigital/uec-dos-management/.github/actions/react_clear_cloudfront_cache@task/DR-930_Implement_appdeploy_intenv
        with:
          code_directory: ${{ inputs.code_directory }}
          distribution_id: ${{ steps.derive_cloudfront_dist_id.outputs.cloudfront_dist_id }}
