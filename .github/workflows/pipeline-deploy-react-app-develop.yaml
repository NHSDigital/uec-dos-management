name: Pipeline Deployment React Application Development Environment

on:
  push:
    branches:
      - main
      - 'task/*'
jobs:
  metadata:
    name: "Get Metadata"
    uses: ./.github/workflows/metadata.yaml

  deploy-infrastructure-apply:
    name: "Apply Deploy Infrastructure"
    needs:
      [
        metadata,
      ]
    uses: ./.github/workflows/deploy-infrastructure.yaml
    with:
      environment: mgmt
      workspace: ${{ needs.metadata.outputs.workspace }}
      domain: "uec-dos-management"
      stacks: "['ui-test-stack']"
      action: apply
      workflow_timeout: 10
    secrets: inherit

  deploy-react-application:
    name: "Deploy react application code"
    needs: [
      metadata,
      deploy-infrastructure-apply,
      # TODO job to build react app
      #TODO job to derive artefact bucket name job
    ]
    uses: ./.github/workflows/deploy-react-app.yaml
    with:
      environment: mgmt
      workspace: ${{ needs.metadata.outputs.workspace }}
      domain: ${{ needs.metadata.outputs.reponame }}
      # TODO Hardcoded hash
      commit_hash: def123
      # Hardcoded artefact bucket name
      artefact_bucket_name: nhse-mgmt-uec-dos-management-artefacts
    secrets: inherit

  test-react-application:
    name: "Run ui tests against react app"
    needs: [
      metadata,
      deploy-react-application
    ]
    uses: ./.github/workflows/ui-test.yaml
    with:
      environment: mgmt
      workspace: ${{ needs.metadata.outputs.workspace }}
    secrets: inherit

  deploy-infrastructure-destroy:
    name: "Destroy Deploy Infrastructure"
    needs:
      [
        metadata,
        deploy-infrastructure-apply,
        deploy-react-application,
        test-react-application,
      ]
    uses: ./.github/workflows/deploy-infrastructure.yaml
    with:
      environment: mgmt
      workspace: ${{ needs.metadata.outputs.workspace }}
      domain: "uec-dos-management"
      stacks: "['ui-test-stack']"
      action: destroy
      workflow_timeout: 10
    secrets: inherit

  slack-notifications:
    needs: [
      metadata,
      deploy-infrastructure-apply,
      deploy-react-application,
      test-react-application,
      deploy-infrastructure-destroy,
    ]
    if: always()
    uses: ./.github/workflows/slack-notifications.yaml
    with:
      env: mgmt
    secrets: inherit

